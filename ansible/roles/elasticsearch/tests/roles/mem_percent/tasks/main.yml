- name: 50% da memoria RAM
  debug: msg={{ ansible_memtotal_mb * 0.5 |int }}

- name: Quantidade de hosts no grupo -1
  debug: msg={{ groups['masters'] |length - 1 }}

- name: Quantidade de hosts no grupo -1
  debug: msg={{ groups['masters'] |length - 1 }}


- name: Calc RAM percent in jinja2
  template:
    src: mem_percent.j2
    dest: /tmp/mem_percent.conf
